{
  "checkpoint_schema_version": "0.1",
  "checkpoint_id": "15-integration",
  "phase": "packaging and end-to-end canary integration",
  "status": "passed",
  "scope": ["tests/integration/**", "fixtures/canary/**"],
  "tdd": {"red": "canonical envelope contract assertions failed before repair", "green": "pytest -q: 93 passed"},
  "invariants": ["fixture bytes pass ingestion and structuring", "integration calls production extraction, validation, and assembly ports directly", "canonical envelopes carry stable version, record_id, and provenance fields", "structured block/span cardinality is enforced", "repeated clean runs produce equal snapshots, items, and citations"],
  "reviewer_status": "pending independent verification",
  "repair": "canonical extraction/formal payloads now expose complete envelopes; GraphEdgeEvent validates required identity fields; checkpoint 05 left unchanged because it is outside exclusive ownership"
}
