{
  "checkpoint_schema_version": "0.1",
  "checkpoint_id": "15-integration",
  "phase": "packaging and end-to-end canary integration",
  "status": "passed",
  "scope": [
    "tests/integration/**",
    "fixtures/canary/**"
  ],
  "tdd": {
    "red": "canonical envelope contract assertions failed before repair",
    "green": "PYTHONPATH=src python -m pytest -q tests/schema tests/extraction tests/formal tests/ingestion: 66 passed"
  },
  "invariants": [
    "fixture bytes pass ingestion and structuring",
    "integration calls production extraction, validation, and assembly ports directly",
    "canonical envelopes carry stable version, record_id, and provenance fields",
    "structured block/span cardinality is enforced",
    "repeated clean runs produce equal snapshots, items, and citations"
  ],
  "reviewer_status": "verified 2026-09-17: fresh clone, uv sync --extra dev + uv run pytest -q -> 209 passed, 0 failed; docs/decision-register.md checked against current code, no drift",
  "repair": "canonical extraction/formal payloads now expose complete envelopes; GraphEdgeEvent validates required identity fields; checkpoint 05 also records its current suite count; explicit run_id updates remain outside ownership",
  "tests": [
    {
      "command": "PYTHONPATH=src python -m pytest -q tests/schema tests/extraction tests/formal tests/ingestion",
      "result": "71 passed"
    },
    {
      "command": "env -u PYTHONPATH python -m pytest -q",
      "result": "209 passed"
    }
  ],
  "next_allowed_task": "final independent verification",
  "current_full_suite": {
    "command": "/home/ian-koratsky/.hermes/bin/uv run --offline --no-project --python cpython-3.11.16 --with pytest,jsonschema python -m pytest -q",
    "result": "209 passed"
  }
}
