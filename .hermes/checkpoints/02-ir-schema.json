{
  "checkpoint_schema_version": "0.1",
  "checkpoint_id": "02-ir-schema",
  "phase": "canonical IR and stage contract freeze",
  "status": "passed",
  "contract": "docs/architecture/stage-contracts-v0.1.md",
  "schema": "schemas/envelope-0.1.json",
  "tests": [
    "PYTHONPATH=src python3 -m pytest tests/schema/test_contracts.py tests/schema/test_resource_bounds_adversarial.py -q: 11 passed",
    "PYTHONPATH=src python3 -m pytest -q: 85 passed"
  ],
  "repairs": [
    "TaxonomySnapshot is a canonical immutable provenance-bearing record",
    "production ports reject wrong record types instead of coercing them",
    "canonical IDs and payloads retain envelope and subclass fields",
    "non-empty string revisions are required for EvidenceSpan and document spans",
    "EvidenceSpan and aggregate source text have canonical UTF-8 byte ceilings",
    "public retrieval request construction sanitizes invalid query only for ID derivation and preserves it for refusal",
    "ExtractionBundle and Formalization carry schema version, stable record ID, run, confidence, uncertainty, diagnostics, and status",
    "propositions retain exact half-open source substring spans"
  ],
  "reviewer_status": "pending independent verification wave after repair integration",
  "next_allowed_task": "independent verification and generation-stage decision"
}
