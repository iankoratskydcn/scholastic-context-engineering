{
  "checkpoint_schema_version": "0.1",
  "checkpoint_id": "14-generation-safety",
  "phase": "generation and injection acceptance tests",
  "status": "red",
  "scope": [
    "tests/generation/**",
    "tests/security/test_generation_injection.py",
    "fixtures/generation/**"
  ],
  "tdd": {
    "red": "targeted suite reaches collection and fails because scholastic_pipeline.generation is not implemented on base 9c46370",
    "green": null
  },
  "invariants": [
    "GenerationRequest and GenerationResult are typed immutable boundary records",
    "accepted claims require exact resolvable Citation provenance",
    "missing, stale, and out-of-scope citations refuse",
    "unsupported claims are UNVERIFIED or refused",
    "evidence is preserved rather than silently truncated",
    "retrieved imperative text cannot change policy, tools, or scope",
    "claim and context budgets are hard bounds",
    "refused retrieval cannot become generated evidence",
    "identical typed inputs produce identical output"
  ],
  "tests": [
    {
      "command": "/home/ian-koratsky/.hermes/bin/uv run --offline --no-project --python cpython-3.11.16 --with pytest python -m pytest -q tests/generation tests/security/test_generation_injection.py",
      "result": "collection failed: ModuleNotFoundError: scholastic_pipeline.generation (expected RED; feature absent)"
    },
    {
      "command": "git diff --check",
      "result": "passed"
    }
  ],
  "reviewer_status": "pending implementation and independent verification",
  "issues": [
    "Generation implementation and public typed API do not exist at base commit; tests cannot execute beyond collection until implementer adds it."
  ],
  "next_allowed_task": "implement generation boundary, then run targeted and full suites"
}
