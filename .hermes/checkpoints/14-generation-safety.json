{
  "checkpoint_schema_version": "0.1",
  "checkpoint_id": "14-generation-safety",
  "phase": "generation and injection acceptance tests",
  "status": "implemented",
  "scope": [
    "src/scholastic_pipeline/generation/**",
    "tests/generation/**",
    "tests/security/test_generation_injection.py",
    "fixtures/generation/**"
  ],
  "tdd": {
    "red": "30 focused generation/injection tests collected; 26 passed and 4 failed against the current implementation on non-sentinel provenance revision mismatch, citation offset alignment, and malformed nested span members",
    "green": "30 focused generation/injection tests passed after repairing the generation boundary; full suite passed with 209 tests"
  },
  "invariants": [
    "GenerationRequest and GenerationResult are typed immutable boundary records",
    "accepted claims require exact resolvable Citation provenance",
    "claim citation text and offsets align exactly to retrieved evidence",
    "extra provenance revisions must match the accepted evidence revision",
    "missing, stale, and out-of-scope citations refuse",
    "multiple evidence items are preserved or explicitly refused, never silently collapsed",
    "Claim citations use a typed immutable tuple",
    "unsupported claims refuse",
    "evidence is preserved rather than silently truncated",
    "retrieved imperative text cannot change policy, tools, or scope",
    "claim and context budgets are hard bounds",
    "refused retrieval cannot become generated evidence",
    "identical typed inputs produce identical output",
    "unsupported generation uses explicit refusal and UNVERIFIED records remain explicit",
    "zero-width and punctuation-shaped retrieved instructions remain inert data",
    "refusal results contain no claims",
    "malformed context members refuse without exceptions",
    "nonempty allowed tools are rejected",
    "provenance text bytes count toward context budget",
    "nested claim and citation records remain immutable"
  ],
  "tests": [
    {
      "command": "uv run --offline --no-project --python cpython-3.11.16 --with pytest python -m pytest -q tests/generation tests/security/test_generation_injection.py",
      "result": "30 passed"
    },
    {
      "command": "uv run --offline --no-project --python cpython-3.11.16 --with pytest python -m pytest -q",
      "result": "209 passed"
    },
    {
      "command": "git diff --check",
      "result": "passed"
    },
    {
      "command": "env -u PYTHONPATH python -m pytest -q",
      "result": "209 passed"
    }
  ],
  "current_full_suite": {
    "command": "/home/ian-koratsky/.hermes/bin/uv run --offline --no-project --python cpython-3.11.16 --with pytest,jsonschema python -m pytest -q",
    "result": "209 passed"
  },
  "reviewer_status": "pending independent verification",
  "issues": [],
  "next_allowed_task": "independent adversarial review and verification"
}
