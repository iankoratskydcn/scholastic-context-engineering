{
  "checkpoint_schema_version": "0.1",
  "checkpoint_id": "09-reference-backend",
  "phase": "reference SQLite occurrence-preserving storage boundary repair",
  "status": "passed",
  "scope": [
    "src/scholastic_pipeline/backends/**",
    "tests/backends/**"
  ],
  "tdd": {
    "red": "focused adversarial manifest-integrity tests failed as expected: duplicate manifest IDs and reordered manifest were accepted before repair",
    "green": "backend adversarial suite and full suite passed after minimal repair"
  },
  "invariants": [
    "complete GraphEdgeEvent envelope survives SQLite round-trip",
    "mixed run IDs/schema versions/provenance scopes rejected",
    "manifest IDs are unique and match occurrence cardinality and ordinal order",
    "manifest and expected count bind completion",
    "failed writes roll back and partial generations are unreadable",
    "retries are idempotent and completed generations immutable",
    "stale and missing provenance rejected",
    "malformed public inputs fail only with StorageError or documented subclasses"
  ],
  "tests": [
    {
      "command": "env -u PYTHONPATH python -m pytest -q",
      "result": "176 passed"
    }
  ],
  "reviewer_status": "pending final independent verification",
  "issues": [],
  "next_allowed_task": "fresh independent verification",
  "current_full_suite": {
    "command": "/home/ian-koratsky/.hermes/bin/uv run --offline --no-project --python cpython-3.11.16 --with pytest,jsonschema python -m pytest -q",
    "result": "179 passed"
  }
}
