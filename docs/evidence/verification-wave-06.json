{
  "schema_version":"0.1",
  "review_wave":"deleg_095765e7",
  "status":"blocked",
  "proof":["117 tests passed in clean archives","cross-process canary deterministic","production canary direct path passed","fixture/checkpoint references parse"],
  "blocking_findings":["ValidationResult accepts semantically empty accepted records","ExtractionBundle accepts accepted records without argument/taxonomy","QuarantinedDocument accepts empty identity and emits invalid envelope","Formalization accepts empty expression","taxonomy provenance compares scope but not exact span identity","invalid run_id can raise through abstention construction","invalid current_revisions and generation options can raise","repeated wheel builds are not byte-identical"],
  "hygiene_findings":["checkpoint counts are stale","16 clean completed worktrees remain untracked"],
  "verdict":"HOLD_RELEASE",
  "next_proof":["enforce direct record semantic invariants and fail-closed invalid run inputs","make wheel reproducible and normalize checkpoints","fresh independent verification"]
}
