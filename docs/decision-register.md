# Decision Register

| Decision | Status | Current bounded default | Evidence / owner action |
|---|---|---|---|
| Target repository and authority | **approved** | This standalone repository owns the canonical IR | User instruction, 2026-09-12 |
| Taxonomy inclusion policy | **blocked** | Named leaves convert; headings/caveats metadata-only; ambiguous bundles remain unresolved | Requires explicit owner confirmation |
| IR semantic depth | **blocked** | Reviewable formal sketches first; executable semantics separate | Requires explicit owner confirmation |
| Storage authority | **blocked** | SQLite + NetworkX reference baseline | Requires explicit owner confirmation |
| Model/data policy | **blocked** | Deterministic local reference; cloud/LLM disabled | Requires explicit owner confirmation |
| Hermes integration boundary | **deferred** | No Hermes-core changes in first slice | Plan non-goal / later gate |

Implementation remains blocked until all blocking rows are resolved.
